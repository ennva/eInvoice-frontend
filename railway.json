{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'echo REACT_APP_BACKEND_URL=$REACT_APP_BACKEND_URL && nginx -g \"daemon off;\"'"
  }
}
